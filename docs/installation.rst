============
Installation
============

At the command line::

    $ easy_install aws_role_credentials

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv aws_role_credentials
    $ pip install aws_role_credentials
